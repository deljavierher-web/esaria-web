# macOS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes

# Editor
.vscode/

# Carpetas locales (no van al repo)
backups/
renders/
exports/

# IA / agentes
.agents/
skills-lock.json

# Claude Code / Ruflo (archivos locales, no van al repo)
.claude/
.claude-flow/
.swarm/
.mcp.json
RUFLO.md
ruvector.db

# Dependencias (por si se añade tooling futuro)
node_modules/

# Local secrets
.env
*.env
experiments/reels-pipeline/.env

# Reels pipeline — archivos generados (no van al repo)
experiments/reels-pipeline/tmp/
experiments/reels-pipeline/output/
experiments/reels-pipeline/*.mp4
experiments/reels-pipeline/*.wav
experiments/reels-pipeline/*.mp3
experiments/reels-pipeline/assets/piper/
experiments/reels-pipeline/.venv/
experiments/reels-pipeline/.venv312/
